name: Build MHI Industrial
on: [push]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Instalar Libs
        run: pip install pywebview pyinstaller

      - name: Compilar EXE
        run: pyinstaller --onefile --noconsole --name "Manutencao_MHI_Pro" app.py

      - name: Gerar Instalador com Atalho
        shell: powershell
        run: |
          $iss = @"
          [Setup]
          AppName=Manutencao MHI Pro
          AppVersion=2.0
          DefaultDirName={autopf}\ManutencaoMHI
          OutputDir=out
          OutputBaseFilename=Instalador_MHI_Pro
          [Files]
          Source: "dist\Manutencao_MHI_Pro.exe"; DestDir: "{app}"; Flags: ignoreversion
          [Icons]
          Name: "{autodesktop}\Manutencao MHI Pro"; Filename: "{app}\Manutencao_MHI_Pro.exe"
          Name: "{group}\Manutencao MHI Pro"; Filename: "{app}\Manutencao_MHI_Pro.exe"
          [Run]
          Filename: "{app}\Manutencao_MHI_Pro.exe"; Description: "Lançar MHI Pro"; Flags: nowait postinstall
          "@
          $iss | Out-File -FilePath setup.iss -Encoding utf8
          iscc setup.iss

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: Instalador-Final
          path: out/Instalador_MHI_Pro.exe
